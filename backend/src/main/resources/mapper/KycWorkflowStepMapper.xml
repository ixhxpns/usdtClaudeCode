<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usdttrading.repository.KycWorkflowStepMapper">

    <!-- 審核員工作量統計 -->
    <select id="selectReviewerWorkloadStats" resultType="map">
        SELECT 
            COUNT(*) as total,
            SUM(CASE WHEN status = 'COMPLETED' THEN 1 ELSE 0 END) as completed,
            AVG(processing_time_minutes) as avg_processing_time
        FROM kyc_workflow_steps 
        WHERE actual_reviewer_id = #{reviewerId}
        AND completed_at BETWEEN #{startTime} AND #{endTime}
    </select>

    <!-- 分頁查詢工作流步驟 -->
    <select id="selectWorkflowStepsPage" resultType="com.usdttrading.entity.KycWorkflowStep">
        SELECT * FROM kyc_workflow_steps
        WHERE 1=1
        <if test="kycId != null">
            AND kyc_id = #{kycId}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="stepNumber != null">
            AND step_number = #{stepNumber}
        </if>
        ORDER BY created_at DESC, step_number ASC
    </select>

</mapper>