<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usdttrading.repository.RoleMapper">

    <!-- 根据权限查找角色 -->
    <select id="findByPermission" resultType="com.usdttrading.entity.Role">
        SELECT * FROM roles 
        WHERE JSON_CONTAINS(permissions, JSON_QUOTE(#{permission})) 
           OR JSON_CONTAINS(permissions, JSON_QUOTE('*'))
           AND is_active = 1 
           AND deleted = 0
        ORDER BY created_at ASC
    </select>

</mapper>